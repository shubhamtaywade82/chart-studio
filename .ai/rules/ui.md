# UI Rule

- Use Vanilla CSS for styling (as per project preference).
- Maintain a clean separation between the charting logic and the UI panels.
- Use `chart-studio`'s internal provider client for data fetching.
- Ensure all components are responsive and follow the dark theme.
- Avoid external UI libraries unless explicitly approved.
