#!/bin/bash

# Trigger: Before the agent summarizes the conversation to save context.
# Purpose: Ensure critical findings are preserved in memory files before they are truncated.

# Example:
# ./scripts/persist-critical-findings.sh
